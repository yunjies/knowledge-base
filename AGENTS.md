# AGENTS.md

## 知识库装载

本工程的知识库文档不是全部默认装载，装载由场景触发：任务执行时按需装载（如 [agent-constraints.md](_meta/agent-constraints.md)——agent 执行规范）。新增知识库文档时，在其对应生产场景的指令处补指路，否则该文档对未被告知的会话不存在。

## 可达执行路径验证

定义**开始与目标（最终结果）**，可以通过**无限递归推演林，来验证出一条**可达执行路径**。

## 约束

执行期禁令（含不写注释、长期价值优先于成本、不以历史来源替代当前状态、不转述可自行取回的事实）的判据在 [Agent 行为约束](_meta/agent-constraints.md)，本节只承载其中的工程落地细则。

- **决策与审核一律走第一性原理 + 辩证**：方案取舍、审核出具的 PASS/FAIL，都必须从可验证的最小事实推出，并写出最强反题。以"现状如此""既有代码这么写""惯例如此""保持一致性"为由的结论一律不成立。展开与判据见[「决策与审核准则」](#决策与审核准则)。
- **取回路径的写法**：凡事实的权威承载在工程本身（源码、配置、目录结构、命令输出）时，写**取回它的路径**而非取值。替代表达按优先级：
  1. **给可复算的命令**：写 `npm test`、`node verify-loader.js …`、`npm pack --dry-run`，让读者现取现得；判据写成性质（"全绿且退出码 0"）而非具体条数。
  2. **指向单一事实源**：写"见 `package.json` 的 `files` 字段""等于 `protocol.ts` 的 `ROUTE_API`"，而不是把值抄一份到这里；抄写即制造第二事实源，必然分叉。
  3. **穷举成员**：写"`files` 列出的每个路径"或把成员逐个点名，让读者自己数。
  4. **改用序号**：编号用于引用（`步骤 1`、`M07`），不承担数量断言。本节是本工程文档内取回路径形态的维护处——[文档撰写规范](_meta
/doc-writing-constraints.md) 的 D02、D03 在此让位。

## 决策与审核准则

适用于两类场合：**（a）为任何非平凡改动做方案取舍**；**（b）审核他方交付并出具结论**（含自审、CRC 轮）。其余章节规定"怎么做"，本节规定"凭什么这么定"。**与前文冲突时，本节优先**。

### 第一性原理：从最小事实出发，不从现状出发

做取舍前先落到不可再分的事实与目标，再从目标往回推方案。三步必须做完：

1. **最小事实**：这个模块真正要满足的需求是什么？**脱离现有实现**来描述它。
2. **被推翻的假设**：现有写法里哪些只是历史偶然（当时的接口、当时的性能、当时的依赖），而非必然？逐条列出并给出裁决。
3. **从目标重推**：若今天从零实现，最直接的解法是什么？它与现状的差值，就是候选改动。

**无效理由（出现在论证中即视为论证未完成）**："现状如此"、"既有代码就是这么写的"、"为了保持一致"、"惯例如此"、"别人也这样"、"改动面太大"、"现在能跑"、"以后再重构"。一致性只有在它本身就是目标时才成立；否则它是待考察的对象，不是论据。

### 辩证：必须写出最强反题

任何方案必须同时给出**支持面与反对面**，且反对面要取最强的那一版，不得立稻草人。固定三问：

- **最强反题**：在什么条件下这个方案是错的？要归因于设计，不要归因于执行不力。
- **证伪条件**：什么可观测的证据出现时，我判定这个方案失败？（**动手前**写下；事后补写无效）
- **弃用代价**：若半年后必须推翻它，代价落在谁身上、有多大？

只列优点的方案按**未论证**处理，不得进入实施。

### 成本条款：短期成本无否决权

- **不算作理由的成本**：本次多花的工时、多写的代码行、多跑一次的测试与构建。它们不构成否决长期正确方案的理由。
- **必须计入的成本**：长期维护负担、概念与复杂度债务、后续演进的阻力、认知负担、故障半径。这类成本高于一切短期便利，是方案取舍的首要分量。
- **裁决顺序**：长期正确 > 长期可演进 > 短期便捷。发生冲突时默认取长期；确要取短期，必须在留痕中写明"这是可偿还的临时债 + 偿还触发条件 + 偿还责任"。
- **唯一例外**：正在发生的不可逆破坏（数据损坏、对外契约破坏、安全面暴露）可先止血。止血动作照样留痕，并显式标注"临时"与归位计划。

### 留痕：可检查的产出，不是态度

非平凡决策必须在答复或知识库条目中留下下面三项，**缺一项即视为该决策无效**：

```
【事实】脱离现状描述的需求与最小事实
【反题】最强反对意见 + 证伪条件
【裁决】选定方案 + 为何长期视角下更优 + 已知代价与偿还计划
```

**小改动豁免**：单文件、无对外契约变化、可一步回滚的改动，可只留一行裁决。豁免**不适用**于接口与契约、数据模型、构建与挂载、跨半协议，以及任何审核结论。

### 审核：结论必须可复算

审核（他人审核、自审、CRC 轮）不得只给结论，必须同时满足：

- **PASS/FAIL 由事实推出**：列出所依据的可验证事实（测试输出、代码路径、实测日志），结论从这些事实重推可复现。
- **反向检查与正向检查同等重要**：正向问"它做对了什么"，反向问"什么情况下它会错而未被发现"。
- **不得以成本为由放行**："时间不够"、"改动面太大"、"影响可控"不是 PASS 的理由。任何会让长期正确降级的缺陷一律 FAIL，并给出修复方向。
- **不得以措辞让步代替论证**：把断言改写成"通常""一般""建议"不构成修复，按 FAIL 未修复处理。
- **禁止空转**：靠放宽断言、删用例、跳过 E2E 换来的绿灯是 FAIL，不是 PASS。

## 目录结构

- `_meta/`：执行规范与文档撰写规范——约束"应当怎么做"。
- `assets/projects/`：被本知识库收录的项目，每个项目一个子目录。项目目录下直接承载该项目的产物：项目自身的独立仓库克隆各占一层子目录（`<项目>/<repository>/`），项目的流程文档为 `<项目>/feature-flow.md`。仓库克隆内部的目录布局（含其自身的 `src/`）由该项目决定，不在本知识库约束范围内。
- `assets/notes/`：长期笔记区——需要跨会话留存的笔记，其用途、边界与写入形态见该目录的 [README](assets/notes/README.md)。

### 项目目录的阅读方式

读一个被收录项目时按层进入，**不要越过层去猜路径**：

1. **先读流程文档**：`<项目>/feature-flow.md` 是该项目的权威流程说明，给出它已实现的主干、分支与失败出口。先读它，再决定要不要进代码；不要用克隆里的 `README.md` 代替它——后者面向该仓库的开发者，不面向本知识库的读者。
2. **再进仓库克隆**：`<项目>/<repository>/` 是该项目自己的仓库（自带 `.git` 与远端），其内部布局由该项目决定。研发事实（源码、配置、测试、构建输入）一律以克隆内为准，从克隆内取回，不在此处复述。
3. **跑测试从克隆内出发**：入口与判据见克隆内的 `tests/README.md`，不依赖本知识库的 cwd 假设。
4. **横切材料另找**：与单个项目流程无关、但需跨会话留存的部署与运维事实在 `assets/notes/`，按该目录 README 的边界取用；`assets/notes/` 不放流程文档的副本。

多项目时的取法：`assets/projects/` 下每个子目录即一个项目，`<项目>` 与 `<repository>` 不必同名（前者是本知识库的收录名，后者是该仓库的克隆名）。

## 测试

目标：证明"改动是对的"，并证到该有的强度。三层测试各自回答不同问题，**按改动性质选层，不要一律跑全量**——选层是为了用对工具，不是为了少跑；**不得以"成本高"为由降层或跳过**（见[「成本条款」](#成本条款短期成本无否决权)）。

被收录项目的测试套件**只维护一套**，且不在本知识库内：它随项目源码存放于项目自己的仓库克隆内（`assets/projects/<项目>/<repository>/tests/`），目录规范按镜像规则——该仓库 `src/` 下每一类代码、配置与产物在测试树中有一处对应节点。跑法与判据取回自该项目的 `tests/README.md`；命令是项目自身入口（`uv run pytest -q`，判据为全绿且退出码 0）。本知识库不再持有任何可达的 `tests/`；新增项目测试写在项目内，不落回知识库。
